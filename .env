# Server name
SERVER_NAME=test-server
# Host nationality
NATION=FIN 
# List game to public hostlist
PUBLIC=no 
# Show game in public hostlist web page (game must also be public)
PUBLIC_WEB=no 

# Uncomment these lines if have account for record database
#REC_MAIL=CHANGEME # Network records account e-mail (trusted host)
#REC_PASSWD=CHANGEME # Network records password (trusted host)